interface:
  display_name: "HomeRail DAG Patterns (compatibility)"
  short_description: "Legacy entry pointing to the unified HomeRail DAG skill."
  default_prompt: "Use $homerail-dag-patterns to select, adapt, and validate a HomeRail DAG design pattern."
