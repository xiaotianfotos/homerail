# E2E Fix 同题成本对照

2026-09-09，独立单次修复 `issue245-single-shot-baseline-1` 已完成可信测试。
这次单次修复成功且更便宜；已有循环证明的是能从失败中恢复，不是必然节省 token。

## 条件和结果

对照与保留的 `issue245-e2e-unseeded-6` 使用相同 #245 issue 快照、基线
`115352c0154b9bdf52fe728ea5f2268cec9eb831`、允许修改范围、冻结测试定义/镜像、
Codex Planner/Qwen Fixer 的原始指令和模型配置。两者都没有输入旧补丁。
对照使用新身份和独立新会话，原生图在第一次可信测试后结束，不能反馈、审查
或发布 PR。实验前已保存设计和评分规则，无论结果如何都保留，不挑选候选。

| 测量范围 | 可信测试结果 | 后端报告 token |
| --- | --- | ---: |
| 独立单次：Planner + Fixer | 第一次两项必需检查均通过 | 68079 |
| 原循环第一次：Planner + Fixer | 失败，异步断言挂载过晚造成未处理拒绝 | 70304 |
| 原循环到首次过测：前两轮 Planner/Fixer + 第一轮 Judger | 第二轮通过 | 150771 |
| 原循环全部三轮，包含审查、Judger、PR/CI 处理 | PR #307 最终同 head 五项 CI 成功 | 432109 |

最后一行覆盖范围更大，不能与单次候选成本直接作效率比。原循环第三轮的修改
未证明修复此前 Windows CI 失败的原因；仍尊重 Reviewer 对无关性的判断，
不把这条实验事实增加为语义否决门槛。

单次对照中，Codex Planner 报告 23082 输入、461 输出，实际回合约 20 秒。
Qwen 报告 13882 输入、30654 输出，实际 runtime 记录输出额度 65536、独立
进程与仅 handoff 工具。该次输出已经超过 8K；不会因此把输出额度重新压回 8K。
Worker 在 handoff 时提前结束事件消费，未保留后端 done 中的 duration，
所以其模型耗时、TTFT 和纯生成速率仍为 unknown，不用整图耗时推算。

## 证据与边界

独立核对了原配置/角色指令、实际原生命令、Codex thread/turn 回执、Qwen
派发身份、候选 Git head/tree、两次真实 Docker 检查的已退出状态/镜像/标签、
原始测试日志、回执摘要和全部六个命令的一次消费。对照候选为
`902984f88e732b1b72f0a603954f48937d064fb0`，没有 PR 或审查票；图的 completed
只表示测量完成。它不能替代最终实现 PR 验收。

私有实验目录保留 `predeclared-comparison.json`、`terminal-native-snapshot.json`、
`cost-report.json`、`comparison-judgment.json`、`comparison-provenance.json`
及原始候选/模型/测试证据。报告只计后端保留的用量，缓存输入不重复相加，
不包含外围诊断、本对话或未知供应商计费。

样本只有一个单次对照和一个先前循环；对照在循环之后测量，未随机或交错执行。
模型、服务负载和时间变化无法排除。可支持的结论是：本地模型可首轮完成该
限定问题；当首轮失败时，原生反馈循环曾完成有效修复。不能据此宣称任意问题
都会收敛，或 DAG 比单次调用普遍更省 token。
